import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      main: "#006666",
      text: "#000000",
    },
    secondary: {
      main: "#fff",
    },
  },
});
export default theme;
